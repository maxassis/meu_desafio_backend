meta {
  name: pegando os dados do usuario
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/users/getUserData
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenValue}}
}

vars:post-response {
  tokenValue: res.body.access_token
}
