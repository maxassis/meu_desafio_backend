meta {
  name: email recuperar senha
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/sendMailRecovery
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenValue}}
}

body:json {
  {
    "email": "furis@teste.com"
  }
}
